<Window x:Class="Notation.Views.Progress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Traitement"
        Width="300"
        Height="100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Text}" FontSize="15"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Percentage}" FontSize="15" TextAlignment="Right"></TextBlock>
        <ProgressBar Name="ProgressBar" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Value="{Binding Value}"></ProgressBar>
    </Grid>
</Window>
